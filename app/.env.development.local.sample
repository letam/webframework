VITE_SERVER_HOST=//localhost:8000
# Set true to upload media directly to S3/R2 via presigned URLs; false stores
# uploads through the backend (the local-dev default).
VITE_UPLOAD_FILES_TO_S3=false
# VITE_SENTRY_DSN=

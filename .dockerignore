fly.toml
.git/
*.sqlite3
venv
.venv
.tmp/

server/.env
server/static
server/uploads

app/.env

# Never copied into an image (each stage installs its own), but still uploaded to
# the remote builder on every deploy.
**/node_modules

**/__pycache__
**/*.sqlite3
**/secret_key.txt
**/.DS_Store
